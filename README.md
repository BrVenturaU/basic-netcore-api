# Basic .NET Core WebAPI
This is a basic WebAPI on .NET Core 5 (.NET 5).

## Database provider
The project use the Entity Framework Core ORM to comunicate with the SQL Server DBMS.

## Docs
Swagger is included by default.
